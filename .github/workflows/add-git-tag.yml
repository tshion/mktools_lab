name: Add Git Tag

on:
  schedule:
    - cron: '0 19 * * *' # 4am JST
  workflow_dispatch:

jobs:
  add_git_tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - run: sh scripts/git-add-tag.sh

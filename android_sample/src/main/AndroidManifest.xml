<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.tshion.mktools_android_sample">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mktools_lab">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".strict_mode.ThreadPolicyBaseActivity"
            android:exported="false" />

        <activity
            android:name=".strict_mode.ThreadPolicyCustomActivity"
            android:exported="false" />

        <activity
            android:name=".strict_mode.VmPolicyBaseActivity"
            android:exported="false" />

        <activity
            android:name=".strict_mode.VmPolicyCustomActivity"
            android:exported="false" />
    </application>
</manifest>
